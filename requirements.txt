# Core — pipeline (B), tutorials (A), classical diagnostics (C)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Notebooks / weekly runner
jupyterlab>=4.0
nbconvert>=7.0
papermill>=2.4          # optional: parametrised / faster runs for pipeline/07

# Part D — neural surrogates (neural/09, neural/10) only. CPU build is fine.
tensorflow>=2.13        # optional: comment out if you are not running Part D
